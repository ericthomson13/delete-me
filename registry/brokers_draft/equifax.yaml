# DRAFT — not loaded by the active registry. See README.md in this dir.

id: equifax
name: Equifax
website: https://www.equifax.com
tier: enterprise_aggregator

opt_out:
  methods: []                       # TODO: confirm — credit bureaus often gate via separate consumer-portal flows
  # email: TODO
  # web_form: TODO
  # postal: TODO
  # instructions_url: TODO

accepts_authorized_agent: true
agent_form_requirements: []
required_pii: [full_name, current_address, dob_full, ssn_last_4]
optional_pii: [prior_addresses, email, phone]
audit_sources: []

statutes: [ccpa_1798_105]
last_verified: 1970-01-01
maintainer: "@needs-verification"
user_submit_only: false
notes: |
  Credit reporting agency. FCRA-regulated; CCPA deletion rights have
  carve-outs for FCRA-covered data, so the deletion scope may be
  narrower than for non-credit brokers. Verifier should also document
  whether marketing-data deletion is offered separately from credit-file
  freezes / disputes.
