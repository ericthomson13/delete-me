"""delete-me FastAPI service.

Thin HTTP layer over core_py/delete_me/cases.py — no business logic here.
Run locally:

    uv run --extra service uvicorn service.app:app --reload

Or via docker-compose (see docker/docker-compose.yml).
"""
