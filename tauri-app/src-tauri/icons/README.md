# Placeholder icons

These are tiny solid-color PNGs generated to satisfy the Tauri build macro
so `cargo check` / `cargo test` work in a fresh clone. **They are not the
product icon.** Replace before any release build with proper artwork (the
`tauri icon` CLI can generate every required size from a single source PNG).
